
const initialState = {
    count:1
}

export const actionTypes = {

}

export const actions = {
}

export function HomeReducer (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}
